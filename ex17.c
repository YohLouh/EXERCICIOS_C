// Ler palavra e substituir 'a' por '@'
#include <stdio.h>
#include <string.h>

void main(){

    char palavra[20];
    printf("Insira uma palavra: ");
    scanf("%s", palavra);

    for (int i = 0; i < strlen(palavra); i++){
        
    }
        
}
